export const onLike = (uid, status) => {
  /**
   * @type {Boolean}
   *  True = like
   *  False =  unlike
   */

  let data = localStorage.getItem('like');

  console.log(data);

  console.log('data');

  //   if (status) {
  // get the previous like count first
  // then increment the value
  // localStorage.getItem({});
  //   }
};
